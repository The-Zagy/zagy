import { PrismaClient } from '@prisma/client';
export default  new PrismaClient();
// todo find a way to provide one one object and close the connection before the program exit and i think it's easy btw
